import { CamelcaseDirective } from './camelcase.directive';

describe('CamelcaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CamelcaseDirective();
    expect(directive).toBeTruthy();
  });
});
